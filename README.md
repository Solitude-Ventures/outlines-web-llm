# WebGPU LLM with Grammar-Constrained Structured Generation

Run [LFM2.5-1.2B](https://huggingface.co/LiquidAI/LFM2.5-1.2B-Instruct-ONNX) entirely in the browser using WebGPU, with **guaranteed valid JSON output** on the first attempt via [outlines-core](https://github.com/dottxt-ai/outlines-core) compiled to WebAssembly.

## How it works

```
                         Browser (single page)
┌──────────────────────────────────────────────────────────┐
│  base.html                                               │
│  ┌────────────────────────────────────────────────────┐  │
│  │  Web Worker                                        │  │
│  │                                                    │  │
│  │  transformers.js ──► model.generate()              │  │
│  │       │                    │                       │  │
│  │       │              logits_processor              │  │
│  │       │                    │                       │  │
│  │       │              outlines-wasm                 │  │
│  │       │              ┌─────────────┐               │  │
│  │       │              │ JSON Schema │               │  │
│  │       │              │     ↓       │               │  │
│  │       │              │   regex     │               │  │
│  │       │              │     ↓       │               │  │
│  │       │              │    DFA      │               │  │
│  │       │              │     ↓       │               │  │
│  │       ▼              │ mask logits │               │  │
│  │  tokenizer ────────► │ per token   │               │  │
│  │  (vocab)             └─────────────┘               │  │
│  └────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────┘
```

1. **JSON Schema → Regex** — outlines-core converts the target schema into a regular expression that matches all and only valid JSON conforming to that schema.
2. **Regex → DFA** — The regex is compiled into a deterministic finite automaton, indexed against the model's vocabulary.
3. **Token-level masking** — At each generation step, the DFA state determines which tokens are allowed. A `LogitsProcessor` sets disallowed token logits to `-Infinity` before sampling.
4. **State advance** — After each token is chosen, the DFA state is advanced, ensuring the next mask is correct.

The result: the model can only produce token sequences that form valid JSON matching the schema. No retries, no post-hoc parsing failures.

## Files

| File | Purpose |
|---|---|
| `base.html` | Single-file app: React UI + Web Worker with transformers.js and outlines-wasm integration |
| `outlines_wasm.js` | JS glue generated by wasm-bindgen |
| `outlines_wasm_bg.wasm` | outlines-core compiled to WASM (~700 KB) |
| `build-outlines-wasm.sh` | Deterministic build script: clones outlines-core, patches for WASM, compiles |
| `serve.py` | Dev server with COOP/COEP headers (required for SharedArrayBuffer) |

## Quick start

```bash
python3 serve.py
# → http://localhost:8765/base.html
```

Load the model (~700 MB, cached persistently in browser), pick a mode, send a message.

## Modes

| Mode | Constraint | Output |
|---|---|---|
| **Plain chat** | None | Free-form text |
| **Sentiment analysis** | Grammar-constrained (outlines DFA) | `{ sentiment, confidence, reasoning }` |
| **Entity extraction** | Grammar-constrained (outlines DFA) | `{ people, organizations, locations, dates }` |
| **Tool calling** | Retry with validation | `[{ name, arguments }]` |

## Rebuilding the WASM module

Requires Rust, wasm-pack, and the `wasm32-unknown-unknown` target.

```bash
./build-outlines-wasm.sh
```

The script clones `outlines-core` at a pinned tag (currently `0.2.14`), applies two patches to make `tokenizers` optional (it uses native code incompatible with WASM), creates the wasm-bindgen wrapper, builds with `wasm-pack`, and copies the output files. The build directory is cleaned up automatically.

## Key dependencies

- [transformers.js v4](https://github.com/huggingface/transformers.js) — ONNX model inference via WebGPU
- [outlines-core](https://github.com/dottxt-ai/outlines-core) — JSON schema → regex → DFA for structured generation
- [wasm-bindgen](https://github.com/rustwasm/wasm-bindgen) — Rust ↔ JS interop for the WASM module
